%%%-------------------------------------------------------------------
%%% @author Flah
%%% @copyright (C) 2014, <COMPANY>
%%% @doc
%%%
%%% @end
%%% Created : 13. Nov 2014 15:37
%%%-------------------------------------------------------------------
-module(quickSortRandom).
-author("Flah").

%% API
-export([]).

%quickSortRandom(Links, Rechts, Array) ->
%  if
%    Links < Rechts ->
%      Teiler = teileRandomisiert(Links, Rechts, Array),
%      quickSortRandom(Links, Teiler - 1, Array),
%      quickSortRandom(Teiler + 1, Rechts, Array);
%    true -> notImplementedYet
%  end.

%teileRandomisiert(Links, Rechts, Array) ->
%  notImplementedYet.